// DEPRECATED — kept for back-compat with existing page imports.
// New code should import directly from `lib/ui`:
//
//   import { Card, Button, StatTile } from '../lib/ui';
//
// Migrate page-by-page; this shim will go away once nothing references it.
export {
  Badge,
  Card,
  EmptyState,
  Modal,
  PageHeader,
  StatTile,
  StatusDot,
  Subnav,
} from './ui';
export type { Tone } from './ui';
